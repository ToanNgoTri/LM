<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Đọc câu hỏi bằng giọng nói ở màn hình Chat AI -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Thanh toán trong ứng dụng (Google Play Billing) cho tính năng Premium -->
    <uses-permission android:name="com.android.vending.BILLING" />

    <!--
      Android 11+ lọc gói theo tầm nhìn: không khai báo phần này thì
      SpeechRecognizer.isRecognitionAvailable() luôn trả về false vì app không
      "nhìn thấy" ứng dụng cung cấp dịch vụ nhận dạng giọng nói.
    -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/t"
      android:roundIcon="@mipmap/t"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
